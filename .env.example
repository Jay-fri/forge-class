VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Ask AI (Phase 3). Server-side only — set via `supabase secrets set
# GEMINI_API_KEY=...`, not read by the client. Deliberately NOT VITE_-prefixed.
GEMINI_API_KEY=
